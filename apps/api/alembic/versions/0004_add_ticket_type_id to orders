"""add ticket_type_id to orders"""

from alembic import op
import sqlalchemy as sa

revision = "add_order_ticket_type_id"
down_revision = "add_checkin_created_at"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("orders", sa.Column("ticket_type_id", sa.String(length=64), nullable=True))


def downgrade() -> None:
    op.drop_column("orders", "ticket_type_id")
